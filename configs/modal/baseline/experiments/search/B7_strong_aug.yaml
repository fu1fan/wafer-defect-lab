# Auto-generated search config: B7_strong_aug
_base_:
  - ../../../base/wm811k_classifier.yaml
  - ../../models/resnet50.yaml

task_mode: multiclass

model:
  arch: resnet50
  in_channels: 1
  pretrained: false
  dropout: 0.2

train:
  epochs: 15
  lr: 0.0005
  weight_decay: 0.0005
  grad_clip: 1.0
  loss_type: focal
  focal_gamma: 2.0
  optimizer: adamw
  scheduler: cosine

data:
  sampler: class_balanced
  batch_size: 64
  augment:
    random_flip: true
    random_rotate90: true
    random_translate_frac: 0.12
    random_scale_min: 0.9
    random_scale_max: 1.1
